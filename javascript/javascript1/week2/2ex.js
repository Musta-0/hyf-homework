// 1. write a program to find the largest number in an arrya
let arr = [2, 45, 3, 67, 34, 567, 34, 345, 123];
let largest = arr[0];
for (let i = 0; i < arr.length; i++) {
    if (arr[i] > largest) {
        largest = arr[i];
    }
}
console.log("the largest number is " + largest);


// 2. write a program (function) that will ask all students whether they are in class today. Then run it
const studentsArray = ["Lucia", "Reenu", "Lokesh", "Violeta"]
let student=studentsArray[0]
for (let i = 0; i < studentsArray.length; i++) 
console.log("Are u in the class " + studentsArray[i] + " ?" );

// 3. Write a program that will let you buy fresh falafels (console.logs a message that you bought a falafel) unless you run out of money
let myMoney = 200
const falafelPrice = 20

while(myMoney=200){
    if(mym)
    console.log("you bought flafel and u have" +200  )
    myMoney++;

}


